import './styles.css'
import { Steps } from 'mdx-deck'
import { CodeSurfer, CodeSurferColumns, Step } from 'code-surfer';
import { shadesOfPurple, vsDark, oceanicNext} from '@code-surfer/themes';
import { BuilderSlideSection } from './components/BuilderSlideSection/BuilderSlideSection.js'
import { Fireworks } from './components/Fireworks/Fireworks.js'
import Button from './components/Button/Button.js'
import { Bottleneck } from './slides/Bottleneck.js'
import { RandomlyPlaced } from './layouts/RandomlyPlaced.js'
import { ImageDeck } from './components/ImageDeck/ImageDeck.js'


import './components/Fireworks/Fireworks.builder.js'

import myTheme from './theme/theme'

export const theme = myTheme

# Code or No-Code: Why not both? 🤷‍♀️

<Steps>

## Building a hybrid infrastructure using Next.js & Builder.io to free up your time

<RandomlyPlaced top={20} left={40}>
  <ImageDeck width={400} height={400} imgSrc='ron.png' />
</RandomlyPlaced>

<RandomlyPlaced top={20} left={40}>
  <ImageDeck width={600} height={500} imgSrc='ron.png' />
</RandomlyPlaced>
</Steps>

---

<RandomlyPlaced top={20} left={40}>
  <ImageDeck width={400} height={400} imgSrc='bottle.png' />
</RandomlyPlaced>

---

# We can ship code 5X faster 🚀

---



---

<div style={{width: '100vw'}}>
  <BuilderSlideSection/>
</div>

---

<CodeSurfer theme={myTheme}>

```js
import React from 'react';
import MuiButton from '@mui/material/Button';

const Button = ({ text, link, type }) => {
  return (
    <MuiButton text={text} href={link} variant={type}>
      {text}
    </MuiButton>
  );
};

export default Button
```

```js 1
console.log(1);
console.log(2);
console.log(3);
```

```js 
console.log(1);
console.log(2);
console.log(3);
console.log(4);
console.log(5);
console.log(6);
```

</CodeSurfer>

---

<CodeSurferColumns themes={[vsDark, shadesOfPurple]}>

<Step subtitle="First Step">

```js
console.log(1);
console.log(2);
```

```js
console.log("what");
console.log("why");
```

```js
console.log("a");
console.log("b");
console.log("3");
```

</Step>

<Step subtitle="Second Step">

```js 2
console.log("ron");
console.log("amir");
```

```js 2
console.log("avi");
console.log("bob");
```

```js 2
console.log("amos");
console.log("bezeloa;");
console.log("cameron");
```

</Step>

</CodeSurferColumns>